#include "Vertex.h"

class Triangle {
private:
    // vertices
    Vertex vertex1;
    Vertex vertex2;
    Vertex vertex3;

public:
    // constructor
    Triangle(Vertex vertex1, Vertex vertex2, Vertex vertex3);
};